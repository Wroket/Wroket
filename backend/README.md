# Backend

Base du backend Node.js/Express en TypeScript.

## Scripts

- `npm run dev` : lance l'API en mode developpement
- `npm run build` : compile TypeScript vers `dist/`
- `npm start` : lance l'API compilee

## Endpoints

- `GET /` : message de statut
- `GET /health` : etat de sante de l'API

